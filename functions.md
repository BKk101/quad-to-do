## 구현된 기능 요약
- 메모 생성: 제목, 내용, 마감일, 카드 색상(기본 팔레트 또는 커스텀 색상)으로 새 메모 생성
- 메모 수정: 메모 리스트 클릭 시 편집 모달에서 제목, 내용, 마감일, 카드 색상 수정
- 메모 삭제: 메모 리스트, 편집 모달, 우클릭 완료 팝오버에서 삭제 가능
- 메모 리스트 정렬: 마감일, 중요도, 긴급도, 생성일, 제목 기준 정렬
- 완료 메모 필터: 메모 리스트에서 완료된 메모 숨김 토글 제공
- 사분면 보드: 중요도와 긴급도를 기준으로 활성 메모를 4분면 보드에 배치
- 축 전환: 보드의 X/Y 축을 `Importance/Urgency` 또는 `Urgency/Importance`로 전환
- 드래그 재배치: 보드에서 카드 드래그로 중요도와 긴급도 값 갱신
- 마감일 기반 긴급도 보정: 마감일이 가까워질수록 표시 긴급도가 점진적으로 증가
- D-Day 배지: 마감일이 있는 메모에 `D-n` / `D+n` 형태 배지 표시
- 긴급도 컬러 도트: 활성 메모와 완료 메모 카드에 긴급도 색상 표시
- 메모 본문 미리보기: 보드 카드 호버 시 메모 본문 툴팁 표시
- 완료 처리: 활성 메모 우클릭 시 팝오버에서 완료 처리
- 완료 목록: 완료된 메모를 별도 영역에 표시하고 완료일 표시
- 완료 상세 보기: 완료 메모 클릭 시 읽기 전용 상세 모달 표시
- 완료 취소: 완료 메모 우클릭 시 다시 활성 메모로 복귀
- 메모 내보내기: 전체 메모를 JSON 파일로 Export
- 메모 가져오기: JSON 파일을 Import하여 기존 메모와 ID 기준으로 병합
- 로컬 저장: IndexedDB에 메모 저장, 로드, 삭제, 전체 교체 처리
